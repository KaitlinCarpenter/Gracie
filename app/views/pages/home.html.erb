          <div class="inner cover">
            <h1 class="cover-heading">Gracie Carpenter</h1>
            <p class="lead">This is Dobby. He probably looks guilty because he recently tried to sneak into the refrigerator.</p>
          <% if user_signed_in? %>
            <p class="lead">
            <%= link_to "Get Started!", about_path, class: "btn btn-lg btn-default" %>
            </p>
          <% else %>
            <p class="lead">
            <%= link_to "Sign in", new_user_session_path, class: "btn btn-lg btn-default" %>
            </p>
          <% end %>
          </div>